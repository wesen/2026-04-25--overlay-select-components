{
  "name": "pyxis-component-extractor",
  "version": "1.1.0",
  "description": "Browser extension for visually selecting and extracting UI components from Pyxis standalone pages",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "clean": "rm -f content_scripts/overlay.js"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.0.0",
    "rollup": "^4.0.0",
    "vite": "^5.0.0"
  }
}
